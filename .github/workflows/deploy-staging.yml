name: Staging unavailable (production only)
on: { workflow_dispatch: {} }
permissions: { contents: read }
jobs:
  staging-not-provisioned:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Explain why no deployment runs
        run: |
          echo 'Naaseh currently has only a production AWS environment.'
          echo 'Create a separate account, DNS name, stage-qualified stacks, OIDC role, rollback, and smoke gate before enabling staging.'
          exit 1
